import styled from 'styled-components';

export const Main = styled.main`
    max-width: 1600px;

    display: flex;
    flex-flow: column;
    align-items: center;

    margin: 0 auto;
    padding: 60px 120px;

    box-sizing: border-box;
`;
